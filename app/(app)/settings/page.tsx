export default function SettingsPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold font-display capitalize">settings</h1>
      <p className="text-sm text-muted mt-2 max-w-md">
        Scaffolded route — follows the same pattern as Products and POS: a Supabase
        query + realtime subscription in a client component. Build this out next
        using those two pages as the template.
      </p>
    </div>
  );
}
